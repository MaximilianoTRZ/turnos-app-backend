import { Router } from "express";

const router = Router();

// implement send email notification with nodemailer
router.post("/NotificationWpp/NotificationWpp", (req, res) => {
  res.send("NOT IMPLEMENTED");
});

export default router;
